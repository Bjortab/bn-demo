<!DOCTYPE html>
<html lang="sv">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Blush Narratives</title>
  <link rel="stylesheet" href="styles.css" />
</head>
<body>
  <!-- Top -->
  <header class="top">
    <div class="row">
      <div class="logo">Blush Narratives</div>
      <button class="ghost sm" id="openConnect" type="button">BlushConnect</button>
    </div>
    <div class="levels" id="levelBar" role="group" aria-label="Erotiknivå">
      <button class="lvl" data-level="1" aria-pressed="true" type="button">1</button>
      <button class="lvl" data-level="3" aria-pressed="false" type="button">3</button>
      <button class="lvl" data-level="5" aria-pressed="false" type="button">5</button>
    </div>
  </header>

  <!-- Spelare -->
  <section class="player card">
    <div class="meta">
      <h2 id="storyTitle">Välkommen till Blush Narratives</h2>
      <p class="muted small" id="storyDesc">Välj nivå och tryck play för röstprov.</p>
    </div>
    <div class="controls">
      <button id="playPause" type="button" aria-label="Spela/Pausa">▶</button>
      <input id="seek" type="range" min="0" max="100" value="0" />
    </div>
  </section>

  <!-- Skapa egen -->
  <section id="createBox" class="container card">
    <h3>Skapa egen berättelse</h3>
    <textarea id="customPrompt" placeholder="Skriv din idé..."></textarea>
    <button class="btn sm" id="generateStory" type="button">Skapa</button>
  </section>

  <!-- Bottennav -->
  <nav class="bottom">
    <button class="active" data-tab="home" type="button">Hem</button>
    <button data-tab="favorites" type="button">Favoriter</button>
    <button data-tab="profile" type="button">Profil</button>
  </nav>

  <!-- BlushConnect -->
  <section id="connect" aria-hidden="true">
    <div class="sheet">
      <div class="sheet-head">
        <h2>BlushConnect</h2>
        <button id="closeConnect" class="ghost sm" type="button">Stäng</button>
      </div>

      <div class="pref">
        <label for="connectLevel">Föredragen nivå</label>
        <select id="connectLevel">
          <option value="1">1 - Oskyldig</option>
          <option value="3">3 - Mellan</option>
          <option value="5">5 - Ingående</option>
        </select>
      </div>

      <div class="pref two">
        <label class="toggle"><input type="checkbox" id="showProfile" checked /> Visa profil</label>
        <label class="toggle"><input type="checkbox" id="acceptMsgs" checked /> Tillåt meddelanden</label>
      </div>

      <h3>Personer online</h3>
      <div class="people" id="peopleList"></div>
    </div>
  </section>

  <!-- Dialog -->
  <dialog id="storyDialog">
    <div class="card">
      <h2 id="dialogTitle"></h2>
      <p id="dialogContent"></p>
      <button class="btn sm" id="closeDialog" type="button">Stäng</button>
    </div>
  </dialog>

  <!-- Script sist (säkerställer att allt finns i DOM) -->
  <script src="app.js"></script>
</body>
</html>